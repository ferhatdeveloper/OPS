name: exfin_ops
description: "EXFIN OPS - Saha Satış Yönetim Sistemi"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  translator: ^1.0.3+1

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1
  freezed_annotation: ^2.4.1
  
  # Local storage and database
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  sqflite_common_ffi: ^2.3.2+1
  path_provider: ^2.1.2
  path: ^1.9.0
  http: ^1.4.0
  intl: ^0.19.0
  dio: ^5.8.0+1
  isar: ^3.0.5
  isar_flutter_libs: ^3.1.0+1
  sqflite_common_ffi_web: any
  uuid: ^4.3.3
  rxdart: ^0.27.7
  connectivity_plus: ^5.0.2
  postgres: ^3.4.8
  material_design_icons_flutter: ^7.0.7296
  device_info_plus: ^9.0.2
  network_info_plus: ^4.0.0
  crypto: ^3.0.3
  


  window_size: any
  universal_html: ^2.2.4
  window_manager: ^0.3.8

  # ============================================================
  # SAHA SATIS - Konum ve Harita
  # ============================================================
  geolocator: ^13.0.1
  flutter_map: ^7.0.2              # OpenStreetMap (ücretsiz alternatif)
  latlong2: ^0.9.1                 # flutter_map koordinat desteği

  # ============================================================
  # SAHA SATIS - Barkod ve Kamera
  # ============================================================
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1

  # ============================================================
  # SAHA SATIS - PDF ve Yazdırma
  # ============================================================
  pdf: ^3.11.1
  printing: ^5.13.2
  blue_thermal_printer: ^1.2.3
  share_plus: ^10.1.2

  # ============================================================
  # SAHA SATIS - Bildirimler
  # ============================================================
  flutter_local_notifications: ^18.0.1

  # ============================================================
  # SAHA SATIS - Arka Plan Servisleri
  # ============================================================
  flutter_background_service: ^5.0.11
  workmanager: ^0.5.2

  # ============================================================
  # SAHA SATIS - Raporlama ve Grafikler
  # ============================================================
  fl_chart: ^0.68.0
  excel: ^4.0.6

  # ============================================================
  # SAHA SATIS - İmza ve Güvenlik
  # ============================================================
  signature: ^5.4.0
  local_auth: ^2.3.0
  permission_handler: ^11.3.1

  # ============================================================
  # SAHA SATIS - UI Yardımcıları
  # ============================================================
  flutter_slidable: ^3.1.1
  shimmer: ^3.0.0
  pin_code_fields: ^8.0.1
  supabase_flutter: ^2.12.0
  nfc_manager: ^3.3.0
  web_socket_channel: ^3.0.1
  barcode_scan2: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.8
  freezed: ^2.4.7
  mockito: ^5.4.4
  test: ^1.24.9
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/translations/
    - assets/flags/
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

dependency_overrides:
  app_links: 6.3.2
  package_info_plus: 8.1.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/OPS_padded_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/OPS_padded_icon.png"

flutter_native_splash:
  color: "#ffffff"
  image: "assets/images/OPS_cropped.png"
  color_dark: "#ffffff"
  image_dark: "assets/images/OPS_cropped.png"
  android_12:
    color: "#ffffff"
    image: "assets/images/OPS_cropped.png"
    color_dark: "#ffffff"
    image_dark: "assets/images/OPS_cropped.png"

